# pubspec.yaml
# 版本: V0.6.2
# 修改日期: 2026-04-10
# 修改目的: 添加uuid依赖用于生成唯一ID
name: my_prediction_app
description: A prediction tracking application
version: 0.6.2+1  # 更新版本号
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # UI
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  # 数据
  supabase_flutter: ^2.1.1
  provider: ^6.1.1
  uuid: ^4.3.0  # 新增：用于生成唯一ID
  intl: ^0.19.0
  # 其他
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.2.2
  # 图表
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
